package Yoorkin/trie

// Values
fn add[A](T[A], String, A) -> T[A]

fn empty[a]() -> T[a]

fn insert[A](T[A], String, A) -> T[A] //deprecated

fn lookup[A](T[A], String) -> A?

// Types and methods
type T[A]
impl T {
  add[A](Self[A], String, A) -> Self[A]
  from_array[A](Array[(String, A)]) -> Self[A]
  insert[A](Self[A], String, A) -> Self[A] //deprecated
  lookup[A](Self[A], String) -> A?
  of[A](FixedArray[(String, A)]) -> Self[A]
}

// Type aliases

// Traits

